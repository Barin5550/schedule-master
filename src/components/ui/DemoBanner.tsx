/**
 * Раньше показывал баннер «демо-режима». Сейчас приложение работает как
 * самостоятельный продукт (данные в localStorage), поэтому баннер скрыт.
 * Если подключить Supabase (`.env.local`), приложение само перейдёт на
 * реальную БД — менять этот компонент для этого не нужно.
 */
export default function DemoBanner() {
  return null;
}
